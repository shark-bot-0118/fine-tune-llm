# Core ML/AI Libraries
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
peft>=0.7.0

# HuggingFace Hub
huggingface_hub>=0.19.0

# Environment and Configuration
python-dotenv>=1.0.0

# Accelerated Training (optional but recommended)
accelerate>=0.25.0

# For Apple Silicon GPU support (MPS)
# torch with MPS support is included in torch>=2.0.0

# For CUDA support (if using NVIDIA GPUs)
# torch with CUDA support should be installed separately if needed
# Example: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Development and Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Optional: For advanced tokenization and text processing
tokenizers>=0.15.0

# Optional: For model quantization and optimization
bitsandbytes>=0.41.0

# Optional: For monitoring and logging
wandb>=0.16.0
tensorboard>=2.15.0

# Optional: For Jupyter notebook support
jupyter>=1.0.0
ipywidgets>=8.0.0

# System requirements
# Python >= 3.8
# For Apple Silicon: macOS >= 12.0 for MPS support
# For CUDA: CUDA >= 11.8 (for PyTorch 2.0+)